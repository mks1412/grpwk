#define T_LEN 105000
#define S_SIZE 20000
#define S_LEN 25
#define MIN(a, b) ((a) < (b) ? (a) : (b))

// 各文字の重み付き頻度マップ
// typedef struct {
//   int a[T_LEN];
//   int b[T_LEN];
//   int c[T_LEN];
// } Map;
